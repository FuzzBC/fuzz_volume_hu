<?xml version="1.0" encoding="utf-8"?>
<!-- No accessibility events needed - VolumeKeyAccessibilityService only
     implements onKeyEvent() to intercept the hardware volume keys.
     canRequestFilterKeyEvents + flagRequestFilterKeyEvents are the two
     pieces that actually matter here; canRetrieveWindowContent stays
     false since this service never reads screen content. A
     typeWindowStateChanged + GLOBAL_ACTION_BACK backstop was tried and
     reverted - see VolumeKeyAccessibilityService's class doc. -->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeNotificationStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagRequestFilterKeyEvents"
    android:canRequestFilterKeyEvents="true"
    android:canRetrieveWindowContent="false"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100" />
