<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Draws the floating tab/panel above every other app on the head unit. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Keeps the overlay service alive with an ongoing notification. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Relaunches the overlay after the head unit reboots (ignition cycle). -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Update checker (GitHub Releases) and its APK install prompt. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Asking the OS to stop killing the service under Doze/App Standby. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- MANAGE_EXTERNAL_STORAGE removed (was here for the optional
         store-log-on-main-storage feature): reports of an instant crash
         with literally nothing logged - not even TraceLog's very first
         line - started right after this was added. That points at
         something killing the process before FuzzVolumeApp.onCreate() can
         even run, which a manifest-declared permission can do (some
         manufacturer security layers auto-kill an app whose permission
         combination - overlay + foreground service + all-files-access -
         matches known spyware/adware patterns). Pulling it out to test
         that theory; the trace log still works fine from the app's own
         external-files folder without it. -->

    <application
        android:name=".FuzzVolumeApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:roundIcon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- The floating tab + panel + theme popup. Started by MainActivity
             once overlay permission is granted, and again on boot. -->
        <service
            android:name=".VolumeOverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Persistent floating volume control overlay for a car head unit" />
        </service>

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- "Block system volume popup" (MainActivity toggle, off by
             default). Only takes over the volume keys when that toggle is
             on - checked fresh from Prefs on every key press, not cached,
             so switching it off in the app takes effect immediately without
             needing to re-grant/restart anything. Requires the user to
             separately enable this service under Android's own
             Accessibility settings - the toggle alone can't do that, only
             guide them there (see MainActivity.toggleBlockNativeVolumeUi()). -->
        <service
            android:name=".VolumeKeyAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>

</manifest>
